{
    "outputDirectory": "dist",
    "functions": {
        "api/**/*.py": {
            "excludeFiles": "{+(.now|.next|_next|node_modules|public|assets|src|dist)/**,yarn.lock,README.md,*.js}"
        }
    }
}
